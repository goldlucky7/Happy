---
name: travel-stampbook
description: 부부 2인용 여행 스탬프북 웹사이트를 제작·배포하는 스킬. 사용자가 "부산 스탬프북 만들자", "전주 여행지 만들어줘", "○○ 스탬프북", "○○ 여행 팸플릿 만들어줘", "여행 스탬프북 새로 만들자" 등 지역 이름 + 스탬프북/여행지/팸플릿 조합으로 요청하면 반드시 이 스킬을 사용할 것. 기존 스탬프북(제주 등)에 장소를 추가·수정하거나 영업시간을 업데이트해달라는 요청에도 이 스킬을 사용한다. 검증된 관광지·맛집·카페 목록 조사 → 기존 제주 템플릿 코드(초성 검색창·세부 카테고리 칩 포함) 재사용 → GitHub Pages 자동 배포까지 전 과정을 사용자 개입 최소화로 진행한다.
---

# 여행 스탬프북 제작 스킬

부부(사용자 + 신랑) 2인용 여행 팸플릿 웹사이트를 만드는 스킬. 제주 스탬프북(https://goldlucky7.github.io/jeju/)과 동일한 구조로 새 지역 버전을 만든다.

## 사용자 성향 (반드시 준수)

- **비개발자, 안드로이드 폰으로만 작업.** 기술 용어 최소화, 결과 위주로 소통
- **번거로운 것 싫어함.** 질문은 꼭 필요한 것만 1~2개로 압축 (지역, 장소 개수 정도)
- 신랑은 Claude 계정 없음 → 반드시 GitHub Pages 링크로 배포 (Claude 게시 링크 금지)
- 로그인·코드입력 없는 자동 연동 필수 (TextDB 10초 폴링 방식 유지)
- **팩트체크 중시**: 모든 장소의 주소·영업시간을 지도/웹검색으로 교차 검증. 미확인 정보는 "매장 확인"으로 표기 (지어내기 절대 금지)

## 완성품 스펙 (제주판 기준)

- 관광지 ~30곳 + 맛집 ~15곳 + 카페 ~15곳 (개수는 사용자와 협의, 20~40대 취향 커버)
- "갔다왔음" 버튼 → 빨간 도장 + 달성률 진행바
- 두 사람 도장·추가 장소 자동 연동 (로그인 없음), ➕ 장소 추가/삭제, 백업/복원
- 맛집·카페 카드에 영업시간/브레이크타임/휴무일 3줄 표시 + 🕐 영업시간표 전체 목록 모달
- 필터 탭 (전체/관광지/맛집/카페/갔다온 곳)
- 🔍 검색창: 이름·지역·태그·해시·설명·주소 통합 검색, 띄어쓰기 무시, **초성 검색 지원** (예: "ㅇㅈㅎㅈㄱ"), ✕ 초기화 버튼, 결과 없으면 안내 문구
- 세부 카테고리 칩 12개 (가로 스크롤, 키워드 기반 필터. 예: 🏛 박물관·전시, 🌊 해변·바다, 🍜 국수·국밥…) — 검색창과 칩은 동시 적용(AND)됨

## 제작 순서

### 1단계. 요구 확인 (질문 최소화)
지역이 명확하면 개수·컨셉만 짧게 확인하고 바로 시작. 기본값: 관광지 30 + 맛집 15 + 카페 15.

### 2단계. 템플릿 다운로드 (처음부터 코딩 금지)
```
GET https://raw.githubusercontent.com/goldlucky7/jeju/main/index.html
```
bash에서 curl로 받는다. 이 파일이 검증된 완성 코드이므로 **PLACES 배열·CHIPS 배열 데이터와 지역 문구·테마색만 교체**하고 로직(검색/초성 변환/연동 등)은 건드리지 않는다.

### 3단계. 장소 조사 + 검증 (가장 중요)
- 웹검색으로 해당 지역 인기 장소 후보 선정 (최신 트렌드 반영, 20~40대 취향)
- 지도 검색 도구(places_search)로 **전 장소의 주소·영업시간·휴무일 검증** (10개씩 묶어 조회)
- 맛집·카페는 open(영업시간)/brk(브레이크타임)/closed(휴무일) 필드 필수. 확인 안 되면 "매장 확인"
- PLACES 항목 구조: `{id, cat(spot|food|cafe), emoji, region, name, tag, hash, desc, addr, time(관광지용) 또는 open/brk/closed(가게용), links:[["네이버지도",NM("이름")],["비짓○○" 또는 관련 링크]]}`
- id는 영문 소문자, 기존 항목과 중복 금지

### 4단계. 코드 수정
- PLACES 배열 전체 교체, 제목/헤더 문구를 새 지역으로 (예: "부산 여행 스탬프북 — 관광지·맛집·카페 60선")
- **CHIPS 배열(세부 카테고리 칩)도 새 지역에 맞게 교체**: 제주판 칩(오름·트레킹, 오션뷰 카페 등)은 제주 전용이므로 지역 특색에 맞는 라벨 10~12개로 재구성 (예: 부산=돼지국밥·밀면, 해수욕장, 야경·전망 / 전주=한옥·전통, 비빔밥·콩나물국밥)
  - 칩의 keys는 name/tag/desc/addr 등 텍스트 포함 매칭이므로, **실제 PLACES 데이터에 등장하는 단어**로 넣어야 작동함. 칩마다 매칭 결과가 1곳 이상인지 확인
- 검색창 placeholder의 초성 예시("예: ㅇㅈㅎㅈㄱ")도 새 지역의 대표 장소 초성으로 교체
- 검색 로직(toCho, matchesSearch, render)은 변경 금지
- 테마색은 지역 특색으로 변경 가능 (제주=청록+감귤). 예: 부산=바다남색+갈매기, 전주=한옥 갈색+먹색
- **TextDB 커플 키는 반드시 새로 생성**: `{지역영문}stamp_` + 랜덤 32자리 hex (예: `busanstamp_a1b2...`)
  - ⚠️ 제주 키(`jejustamp_3c5491d5379e4abce2a377bae7987b6c`)를 재사용하면 데이터가 섞이므로 절대 금지
- fetch→병합→push 패턴, 10초 폴링, visibilitychange 로직은 변경 금지
- 수정 후 반드시 `node --check`로 문법 검증

### 5단계. 배포 (같은 저장소, 새 파일)
새 지역은 **기존 jeju 저장소에 `{지역영문}.html`로 추가**한다.
- 배포 주소: `https://goldlucky7.github.io/jeju/busan.html` 형식
- **배포 방법 (순서대로 시도)**:
  1. 세션에 GitHub 연동 도구(mcp/gh 등)가 있고 goldlucky7/jeju 저장소에 접근 가능하면 **토큰 없이 그 도구로 직접 커밋·푸시** (가장 간단)
  2. 안 되면 GitHub Contents API 사용:
```
1) GET https://api.github.com/repos/goldlucky7/jeju/contents/{파일명}  → 기존 파일이면 sha 확보 (신규면 sha 생략)
2) PUT 같은 주소, body: {"message":"...", "content":"<base64>", "sha":"<있을 때만>"}
   헤더: Authorization: Bearer <PAT>
```
- **PAT(토큰)**: 🔒 보안상 토큰은 이 파일에 저장하지 않는다. 이 스킬이 담긴 저장소는 공개(public)라서 토큰을 적어두면 GitHub이 자동으로 무효화한다. Contents API가 필요하면 사용자에게 "예전에 만든 jeju 배포용 토큰을 붙여넣어 주세요"라고 짧게 요청할 것
  - 토큰 사양: fine-grained, jeju 저장소 한정, Contents R/W + Pages R/W (현재 발급본 만료: 2026-09-27)
  - 401/403 에러가 나면(만료·무효화): 사용자에게 새 fine-grained 토큰 발급을 스크린샷 단계별로 안내 (jeju 저장소 한정, Contents+Pages R/W 권한)
  - ⚠️ 받은 토큰을 이 스킬 파일이나 저장소 어디에도 다시 적지 말 것
- 업로드 후 1~2분 내 자동 재배포. 사용자는 아무것도 안 해도 됨

### 6단계. 마무리
- 완성 링크 전달 + "이 링크를 신랑에게 보내기만 하면 연동 끝" 안내
- 추가한 장소 요약 (지역별·카테고리별로 간단히)
- 영업시간 미확인("매장 확인") 장소 목록 고지

## 기존 스탬프북 수정 요청일 때

- 제주: `index.html`, 다른 지역: 해당 `{지역}.html`을 raw에서 받아 수정 후 같은 방식으로 푸시
- 장소 추가 시에도 주소·영업시간 검증 필수
- 장소 추가 시 세부 카테고리 칩에 걸리는지 확인 — 어떤 칩에도 안 걸리면 해당 칩 keys에 그 장소의 단어를 추가 (칩 로직 자체는 수정 금지)
- TextDB 키·연동 로직은 절대 변경 금지 (바꾸면 기존 도장이 전부 사라짐)

## 주의사항

- TextDB는 30일간 접속 없으면 데이터 삭제됨 (사이트를 열기만 해도 타이머 리셋) — 오래 안 쓴 지역이면 사용자에게 알려줄 것
- 저장 시 항상 "최신 fetch → 내 변경만 병합 → push" 패턴 유지 (동시 수정 덮어쓰기 방지)
- 장소 설명(desc)은 2~3문장, 실용 팁(웨이팅, 예약, 동선) 포함
